query DEEM2String_query = DEEM!Net.allInstances()->
	select(e | e.oclIsTypeOf(DEEM!Net))->
	collect(x | x.toString().writeTo('platform:/resource/CHESSReviewDemo/models/' + x.Name ));

uses DEEM2String;

